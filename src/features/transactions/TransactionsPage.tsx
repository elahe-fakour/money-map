import { PagePlaceholder } from '../shared/PagePlaceholder'

export function TransactionsPage() {
  return (
    <PagePlaceholder
      title="Transactions"
      eyebrow="Track money movement"
      description="Add, review, search, filter, and sort income, expenses, and transfers."
    />
  )
}
